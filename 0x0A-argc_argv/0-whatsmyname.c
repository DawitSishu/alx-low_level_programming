#include <stdio.h>
/**
* main - entry point
* @argv: array of command lines
* @argc: size of argv
* Return: 0
**/
int main(int argc, char **argv)
{
if (argc >= 0)
{
printf("%s\n", argv[0]);
}
return (0);
}
